VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Google Identity Services client ID (public, safe to expose client-side).
# Same value as the Google Client ID configured in Supabase Dashboard →
# Authentication → Providers → Google.
VITE_GOOGLE_CLIENT_ID=
